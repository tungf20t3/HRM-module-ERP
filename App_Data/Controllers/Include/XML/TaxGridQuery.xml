<queries>
  <query event="Loading">
    <text>
      <![CDATA[
select @@fieldExternal from @@prime$partition$current a left join dmtk b on a.tk_thue_no = b.tk where @@whereClause and a.loai_ct not in ('0', '#') order by @@orderByClause
]]>
    </text>
  </query>
</queries>